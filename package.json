{
  "name": "ladder-bidding",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @bellwether/protocol && npm run build --workspace @bellwether/content && npm run build --workspace @bellwether/game && npm run build --workspace @bellwether/testkit && npm run build --workspace @bellwether/server && npm run build --workspace @bellwether/web",
    "check": "npm run typecheck && npm run test && npm run docs:check && npm run build",
    "agent:example": "node --conditions=development --import tsx examples/playtest-agent.ts",
    "dev": "npm run dev --workspace @bellwether/server",
    "dev:web": "npm run dev --workspace @bellwether/web",
    "docs:check": "node scripts/check-docs.mjs",
    "start": "npm run start --workspace @bellwether/server",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "npm run typecheck --workspaces --if-present && tsc -p examples/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.2",
    "vite": "^7.0.6",
    "vitest": "^3.2.4"
  }
}
